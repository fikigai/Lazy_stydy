const taskService = require('../src/services/taskService');

describe('Task Validation (QA Lab #8)', () => {
  test('Should throw error if user has more than 3 In Progress tasks', async () => {
    // Тут ми будемо імітувати (mock) базу даних і перевіряти обмеження
  });
});